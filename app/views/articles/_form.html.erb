<%= form_for @article do |f| %>

	<%= f.label :title %>
	<%= f.text_field :title %> <br />

	<%= f.label :body %>
	<%= f.text_field :body %> <br />

	<%= f.label :published_at %>
	<%= f.date_field :published_at %> <br />

	<%= f.submit %>
<% end %>